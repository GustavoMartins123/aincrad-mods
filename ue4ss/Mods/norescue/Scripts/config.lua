-- norescue overrides — same keys as DEFAULTS in main.lua; delete a line to use
-- the default. Types must match or the override is ignored.
return {
    -- ENABLED = true,
    -- SAFETY_NET_MS = 5000,
    -- DEATH_LANDING_HEIGHT = 1000000000.0, -- 0 = keep the game's 780
    -- DEBUG_LOGS = false,
}
